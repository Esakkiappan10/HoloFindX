<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.holofindx">

    <!-- ================= PERMISSIONS ================= -->

    <!-- Internet (React Native, Metro, APIs) -->
    <uses-permission android:name="android.permission.INTERNET" />
<<<<<<< HEAD
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
=======
>>>>>>> 557c779 (Initial commit)

    <!-- Camera (ARCore requirement) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Optional but recommended for AR performance -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ================= FEATURES ================= -->

    <!-- ARCore-capable camera (required = true means Play Store filters devices) -->
    <uses-feature
        android:name="android.hardware.camera.ar"
        android:required="true" />

    <!-- ================= APPLICATION ================= -->

    <application
<<<<<<< HEAD
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
<<<<<<< HEAD
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
=======
      android:theme="@style/AppTheme">
>>>>>>> 557c779 (Initial commit)
      <activity
        android:name=".MainActivity"
=======
        android:name=".MainApplication"
>>>>>>> 93febe5 (phase-2)
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <!-- ARCore REQUIRED declaration -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required" />

        <!-- ================= REACT NATIVE MAIN ACTIVITY ================= -->

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- ================= NATIVE AR ACTIVITY ================= -->

        <activity
            android:name="com.holofindx.ar.ARActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:screenOrientation="portrait"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

    </application>

</manifest>
